{
  "name": "integro",
  "version": "0.2.2",
  "main": "dist/index.ts",
  "module": "dist/index.ts",
  "type": "module",
  "files": ["dist"],
  "scripts": {
    "build": "tsc && bun build ./src/server.ts --outdir ./dist --target node && bun build ./src/client.ts --outdir ./dist --target browser",
    "prepublish": "bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/nodemon": "^1.19.6",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "msgpackr": "^1.10.1",
    "proxy-deep": "^4.0.1"
  }
}
